to install sbt, scala, libs etc,
run: sh install.sh

output of: cald.rb --local MovieSimilarities.scala --input ./ratings.csv --output ./someOutputFile.tsv
 is also included in the repo for the convenience.
